<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.spring.shopagent.h2mapper.H2Mapper">

    <select id="selectNow" resultType="java.lang.String">
        SELECT NOW()
    </select>

    <update id="updateDatabaseInfo" parameterType="map">
        UPDATE database_info
        SET ai_provider = #{aiProvider}, token = #{token}, database_type = #{databaseType}, url = #{url},
            db_name = #{dbName}, db_user_name = #{dbUserName}, db_password = #{dbPassword}, db_port = #{dbPort},
            user_name = #{userName}, user_email = #{userEmail}
    </update>

    <select id="selectDatabaseInfo" resultType="org.spring.shopagent.info.dto.DatabaseInfoDTO">
        SELECT id, ai_provider, token, database_type, url, db_name, db_user_name, db_password, db_port,
               user_name, user_email, create_date, update_date, data_type
        FROM database_info
    </select>

    <update id="deleteDatabaseInfo">
        UPDATE database_info
        SET ai_provider = '', token = '', database_type = '', url = '',
            db_name = '', db_user_name = '', db_password = '', db_port = 0,
            user_name = '', user_email = ''
    </update>

    <insert id="insertSearchConfig" parameterType="map">
        INSERT INTO search_config (type ,tool_name, description, table_name, product_name, product_image)
        VALUES ('product', #{toolName}, #{description}, #{tableName}, #{productName}, #{productImage});
    </insert>

    <select id="selectSearchConfigByIdx" parameterType="Long" resultType="org.spring.shopagent.setting.dto.SearchRequestDTO">
        SELECT idx AS idxSearchConfig, tool_name AS toolName, description, table_name AS tableName,
               product_name AS productName, product_image AS productImage
        FROM search_config
        WHERE idx = #{idxSearchConfig}
    </select>

    <update id="updateSearchConfig" parameterType="map">
        UPDATE search_config
        SET tool_name = #{toolName}, description = #{description}, table_name = #{tableName}, 
            product_name = #{productName}, product_image = #{productImage}
        WHERE idx = #{idxSearchConfig}
    </update>

</mapper>


