<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.spring.shopagent.mapper.ShopMapper">

    <select id="selectNow" resultType="java.lang.String">
        SELECT NOW()
    </select>

    <select id="selectVersion" resultType="java.lang.String">
        SELECT VERSION()
    </select>

    <select id="selectDatabaseList" parameterType="map" resultType="String">
        SELECT schema_name
        FROM information_schema.schemata
        WHERE schema_name NOT IN ('mysql', 'information_schema', 'performance_schema', 'sys');
    </select>

    <select id="selectDatabaseCheck" parameterType="map" resultType="String">
        SELECT schema_name
        FROM information_schema.schemata
        WHERE schema_name = #{databaseName};
    </select>

    <select id="selectTableList" resultType="String">
        SELECT table_name
        FROM information_schema.tables
        WHERE table_schema = DATABASE()
        AND table_type = 'BASE TABLE'
        ORDER BY table_name
    </select>

    <select id="selectTableColumns" parameterType="map" resultType="org.spring.shopagent.setting.dto.TableColumnResponseDTO">
        SELECT 
            column_name as columnName, data_type as dataType, is_nullable as isNullable,
            column_default as columnDefault, column_comment as columnComment
        FROM information_schema.columns
        WHERE table_schema = DATABASE()
        AND table_name = #{tableName}
        ORDER BY ordinal_position
    </select>
</mapper>
